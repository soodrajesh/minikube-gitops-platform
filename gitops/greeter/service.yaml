apiVersion: v1
kind: Service
metadata:
  name: greeter
  namespace: backend
  labels:
    app: greeter
spec:
  selector:
    app: greeter
  ports:
    - name: http
      port: 80
      targetPort: 8080
